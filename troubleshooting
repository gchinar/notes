<thinking>This is a critical reveal. The whole architecture I was diagnosing was wrong — it's neither ALB OIDC nor NextAuth. It's a custom flow:

1. Backend handles SSO (AD)
2. Backend generates its own JWT
3. Backend passes JWT to frontend via URL parameter (query string, like `?token=eyJ...` or maybe in a redirect)
4. Frontend stores it as a cookie

And then SSR reads the cookie to build the Authorization header — but gets `undefined`.

So now the bug is in this custom handoff. Let me think about where `undefined` could come from in THIS flow:

The flow is: backend redirects to frontend with `?token=xxx` → frontend reads the URL param → frontend writes it to a cookie → later, SSR reads that cookie → SSR builds `Bearer ${token}`.

The `undefined` means: at SSR read time, the cookie either (a) doesn't exist, (b) exists but the value is empty/undefined, or (c) exists under a different name than SSR reads.

Given this custom flow, the most likely failure points:

**A. The URL-param → cookie write happens client-side, but SSR reads before/without it.**
This is the big one. If the backend redirects to the frontend with `?token=`, and the frontend reads `window.location.search` (client-side only) and sets the cookie via `document.cookie` (client-side only) — then:
- The very first request (the redirect landing) is SSR. At that moment, there's no cookie yet (it hasn't been written — the JS hasn't run). SSR tries to read the cookie → undefined.
- The token is sitting in the URL query param, which SSR *could* read from `req.query`, but the code is reading the cookie instead.

So there's a classic ordering problem: token is in the URL on the SSR request, but the code only looks at the cookie, which only gets populated after client-side hydration.

**B. The cookie write itself produces "undefined".**
If the client does something like:
```js
const token = getQueryParam('token'); // returns undefined if param name mismatch
document.cookie = `token=${token}`; // writes literally "token=undefined"
```
Then the cookie value is the string "undefined" — which matches EXACTLY the symptom they reported earlier ("the value is literally the js converted to string 'undefined'").

This is huge. They said the value is literally "undefined" stringified. That means:
- Somewhere, `undefined` got interpolated into a string.
- If it's the cookie that contains "undefined", then the cookie WRITE captured an undefined value.
- That means at write time, the token was undefined → the URL param read returned undefined.

So the bug may actually be earlier than I thought: the URL param extraction returns undefined, writes "undefined" to the cookie, and then SSR reads "undefined" from the cookie and builds "Bearer undefined".

Wait — but they also said earlier "has cookie true but token value is undefined" and "there is no JWT token to decode, the value is literally the js converted to string 'undefined'". 

Let me reconcile. The cookie EXISTS (has cookie true) but its value is the string "undefined". That's consistent with: the cookie write happened, but with an undefined value. Which means the URL-param read failed.

Common reasons the URL-param read fails:
1. **Param name mismatch.** Backend sends `?token=` but frontend reads `accessToken` or `jwt` or `access_token`. Or vice versa.
2. **The token is in the URL fragment (`#token=`) not the query (`?token=`).** Fragments are NOT sent to the server and are read differently client-side (`window.location.hash` not `window.location.search`). If backend puts it after `#` and frontend reads `?`, → undefined.
3. **Reading the URL param server-side when it's only there client-side, or timing.** 
4. **The redirect already consumed/stripped the param.** E.g., a middleware redirect drops query params. Or the param was URL-encoded and decoding failed.
5. **The cookie write captured the param before it was available** (React state timing — reading from a hook that hasn't populated).

But actually, the simplest and most likely: the param NAME is wrong, so the read returns undefined.

Let me also consider: is reading from a URL param and storing in a JS-readable cookie even the right architecture? There are real problems with this:
- Token in URL is a security smell (logged in browser history, server access logs, referer headers). But that's a separate concern from the bug.
- If the cookie is set client-side via document.cookie, it's NOT HttpOnly, so SSR *can* read it (it's sent with requests). So SSR reading the cookie should work IF the cookie has a real value. The problem is the value is "undefined".

So the chain of causation, refined:
1. Backend redirects to frontend with token in URL (`?token=eyJ...` or `#token=` or some param name).
2. Frontend client-side code reads the param — **but reads the wrong name or wrong location (query vs fragment)** → gets undefined.
3. Frontend writes `document.cookie = "X=undefined"` → cookie now literally contains "undefined".
4. SSR (or client) reads the cookie → gets the string "undefined".
5. Builds `Bearer undefined`.

The decisive diagnostic: look at the actual redirect URL the backend sends, and the actual cookie value, and compare the param name the frontend reads.

Let me give them concrete steps:

1. **Capture the actual redirect URL.** In the browser, Network tab, find the redirect FROM the backend TO the frontend (the 302, or the landing URL). Look at the EXACT query string. Is the token there? Under what param name? Is it after `?` or `#`?

2. **Find the frontend code that reads the param and writes the cookie.** Check:
   - What param name does it read?
   - Does it read `window.location.search` (query) or `window.location.hash` (fragment)?
   - Does it guard against undefined before writing the cookie?

3. **Compare param names.** This is probably the bug — mismatch between what backend sends and what frontend reads.

Let me think about whether there's anything in the K8s environment relevant here... Not really. This is purely an app-level handoff bug. The redirect could theoretically be affected by ALB if the ALB rewrites URLs, but unlikely. I'll keep focus on the app.

Actually, one more environment-relevant thing: if there's an ALB doing path-based routing or the redirect URL is constructed with an internal hostname vs external, the redirect could land wrong. But the symptom (cookie contains "undefined") points squarely at param-read failure, not routing.

Let me also reconsider: they said SSR reads the cookie. If the cookie is set by `document.cookie` (non-HttpOnly), and SSR reads it from `req.headers.cookie`, that works — non-HttpOnly cookies are still sent to the server. So the architecture *can* work. The bug is the value.

But wait — there's a subtle timing issue too. The FIRST time the user lands (right after backend redirect), the URL has the token but the cookie doesn't exist yet (client JS hasn't run to set it). If SSR runs on that first landing and tries to read the cookie, it's empty. The cookie only gets set after hydration. So on subsequent navigations the cookie exists. But the value being "undefined" suggests the write happened but with bad data — so it's past the timing issue and into the value issue.

So my primary hypothesis: **param name or query-vs-fragment mismatch causes the client read to return undefined, which gets written to the cookie as the string "undefined".**

Secondary: **the cookie write isn't guarding undefined**, which is the proximate cause of the literal "undefined" string regardless.

Let me write this up. I should:
- Reframe: the architecture is now clear, and it's a custom token handoff, not ALB/NextAuth. Acknowledge this changes the diagnosis.
- Point out the cookie containing literal "undefined" means the WRITE captured undefined — so the bug is upstream of the cookie, at the URL-param read.
- Give the decisive diagnostic: capture the real redirect URL, check param name and query-vs-fragment.
- List the specific likely causes in this flow.
- Give the fix for the proximate symptom (guard the write) and the root cause (match the param).

I should be concise but thorough. This is a debugging conversation; the user is technical and we've been going back and forth narrowing. I've been giving fairly long structured responses and the user keeps engaging, so the format is working. But I should make sure I'm leading with the key insight (the architecture reveal changes things, and the cookie value being "undefined" localizes the bug to the URL-param read) and not burying it.

Let me be careful not to over-format. I'll use some bold for the key diagnostic branches but keep it readable. Given the technical debugging nature and that the user has responded well to structured responses with code snippets, I'll keep a similar format but tighten it.

Let me think about what's truly decisive here and lead with it. The single most important realization: **the cookie contains the literal string "undefined", which means at the moment the frontend wrote the cookie, the value it had was `undefined`. Since the frontend gets the token from the URL parameter, the URL-parameter read is what's returning undefined.** So we've moved the bug one more hop upstream — from "SSR can't read the cookie" to "the cookie was poisoned at write time because the URL param read failed."

The decisive check: look at the actual redirect URL from the backend and the param name the frontend reads.

Let me write it.

Language check: English.That reveal changes the diagnosis completely — this is neither ALB OIDC nor NextAuth, it's a **custom token handoff**, and it relocates the bug one more hop upstream. Here's the key deduction:

You confirmed the cookie's value is the literal string `"undefined"`. A cookie can only contain that if, **at the moment the frontend wrote it, the value it had was `undefined`.** And the frontend gets that value from the URL parameter. So the failing step isn't SSR reading the cookie — SSR is faithfully reading exactly what was stored. The bug is at the **write**: the URL-parameter read returned `undefined`, and that got stringified straight into the cookie.

So the chain is now:

```
backend redirect (?token=eyJ...) → frontend reads URL param → gets undefined → document.cookie = "token=undefined" → SSR reads "undefined" → Bearer undefined
```

Everything downstream is working correctly on poisoned data. Fix the read, and the rest of the chain you've already built will carry a real token.

**Decisive check — capture the actual redirect URL.** Browser → Network tab → find the redirect *from* the backend *to* the frontend (the 302, or the landing request). Look at the raw URL and answer two things:

1. **What is the exact parameter name?** Backend might send `?token=`, `?access_token=`, `?jwt=`, `?accessToken=` — and the frontend code reads a *different* name. A mismatch returns `undefined`. This is the most common cause.
2. **Is the token after `?` or after `#`?** This one's a classic and very easy to miss:
   - `?token=...` is a **query parameter** → read with `new URLSearchParams(window.location.search)` (and it *is* sent to the server).
   - `#token=...` is a **URL fragment** → it is **never sent to the server**, and must be read with `window.location.hash`. If the backend puts it after `#` and the frontend reads `search` (or vice versa), the read returns `undefined`.

Then find the frontend code doing the read+write and compare:

```js
// what name / location does this actually read?
const params = new URLSearchParams(window.location.search); // query
const token = params.get('token');        // ← does 'token' match the backend's param name?
// or, if backend used a fragment:
// const token = new URLSearchParams(window.location.hash.slice(1)).get('token');

document.cookie = `token=${token}`;        // ← if token is undefined, this writes the literal "undefined"
```

**Two fixes, both needed:**

**1. Guard the write (kills the misleading symptom immediately).** Never let `undefined` become a cookie:

```js
if (!token) {
  console.error('No token in redirect URL — params:', window.location.search, window.location.hash);
  // bounce back to login rather than poisoning the cookie
} else {
  document.cookie = `token=${token}; path=/; Secure; SameSite=Lax`;
}
```

The log line tells you instantly whether the token is in `search`, in `hash`, or genuinely absent.

**2. Match the read to what the backend actually sends** — correct param name and correct query-vs-fragment. That's the root cause; once the read returns the real JWT, the cookie holds it, SSR reads it, and `Bearer eyJ...` reaches the backend, which will now validate.

A couple of things worth flagging on this architecture while you're in here:

- **Token in a URL is a real security exposure** — it lands in browser history, server/ALB access logs, and any `Referer` header. If you can, switch the handoff so the backend sets the cookie itself on the redirect response (`Set-Cookie`, ideally `HttpOnly`) instead of passing the token through the URL for JS to store. That removes this entire class of bug *and* the exposure. (If you go `HttpOnly`, remember SSR reads it from `req.headers.cookie` server-side, not `document.cookie`.)
- **One-render timing trap to be aware of:** on the very first landing after the redirect, the URL has the token but the cookie doesn't exist yet (the client write hasn't run). If SSR runs on that first paint and reads the cookie, it's empty until after hydration. Your "value is literally undefined" symptom is past this — it's a write-value problem, not a timing one — but it's the next thing that bites once the value is fixed, so guard SSR for "cookie not present yet" too.

Paste the redirect URL's query/fragment (you can redact the token itself, just keep the **param name** and whether it's `?` or `#`) and the few lines that read it, and I'll confirm the exact one-line fix.