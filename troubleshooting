Two things:

**1. The JWT claims.** Decode the `token` cookie value (paste it at jwt.io, or `echo PASTE | cut -d. -f2 | base64 -d`). Send me the **`aud`, `iss`, and `exp`** fields. Redact the values if you want — I just need to see what they are.

**2. The backend's rejection reason.** Logs from the backend pod for the `/user/me` call that now arrives with the Bearer token:

```
kubectl logs -n ... | grep -iE 'user/me|401|403|400|token|aud|iss|signature|expired'
```

Send whatever that returns.